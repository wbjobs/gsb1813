{
  "name": "csp-auto-gen",
  "version": "1.0.0",
  "description": "根据页面资源自动生成 CSP：资源收集 + 策略生成 + 报告 + 收紧（PerformanceObserver + CSP + Web Worker）",
  "type": "module",
  "main": "src/csp-core.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
